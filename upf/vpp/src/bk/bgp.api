option version = "0.1.0";
import "vnet/interface_types.api";

/** API to enable/disable BGP on an interface */
autoreply define bgp_enable_disable {
    u32 client_index;
    u32 context;
    bool enable_disable;
    vl_api_interface_index_t sw_if_index;
};

